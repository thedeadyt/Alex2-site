{# templates/partials/_navbar.html.twig #}

<header>
    <nav class="px-6 py-4 overflow-x-hidden shadow" style="background-color: var(--color-white); color: var(--color-black);">
        <div class="flex items-center justify-between mx-auto max-w-7xl">
            <!-- Logo -->
            <a href="{{ path('home') }}" class="flex items-center space-x-2">
                <img src="{{ asset('asset/img/logo.png') }}" alt="Logo" class="w-12 h-12" />
                <span class="font-bold text-l" style="color: var(--color-black); font-family: var(--font-heading);">&lt;Alex²/&gt;</span>
            </a>

            <!-- Bouton mobile -->
            <div class="md:hidden">
                <button id="burger-toggle" class="text-gray-700 focus:outline-none">
                    ☰
                </button>
            </div>

            <!-- Desktop nav animé par React -->
            <div id="react-navbar" class="justify-end hidden w-full md:flex"></div>
        </div>

        <!-- Mobile menu statique -->
        <div id="mobile-menu" class="hidden mt-4 space-y-2 md:hidden">
            <a href="{{ path('home') }}" class="block px-4 py-2 text-black bg-white rounded shadow">Accueil</a>
            <a href="{{ path('projets') }}" class="block px-4 py-2 text-black bg-white rounded shadow">Projets réalisés</a>
            <a href="{{ path('services') }}" class="block px-4 py-2 text-black bg-white rounded shadow">Nos services</a>
            <a href="{{ path('apropos') }}" class="block px-4 py-2 text-black bg-white rounded shadow">À propos</a>
            <a href="{{ path('contact') }}" class="block px-4 py-2 text-black bg-white rounded shadow">Contact</a>
        </div>
    </nav>
</header>
